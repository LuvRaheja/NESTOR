spring.cloud.function.definition=plannerFunction
spring.main.web-application-type=${WEB_TYPE:servlet}

# ── Bedrock ──────────────────────────────────────────────────────────────────
nestor.bedrock.model-id=${BEDROCK_MODEL_ID:openai.gpt-oss-120b-1:0}
nestor.bedrock.region=${BEDROCK_REGION:us-east-1}

# ── Aurora Data API ──────────────────────────────────────────────────────────
nestor.aurora.cluster-arn=${AURORA_CLUSTER_ARN:}
nestor.aurora.secret-arn=${AURORA_SECRET_ARN:}
nestor.aurora.database=${DATABASE_NAME:alex}
nestor.aurora.region=${DEFAULT_AWS_REGION:us-east-1}

# ── Child Lambda function names ──────────────────────────────────────────────
nestor.planner.tagger-function=${TAGGER_FUNCTION:nestor-tagger}
nestor.planner.reporter-function=${REPORTER_FUNCTION:nestor-reporter}
nestor.planner.charter-function=${CHARTER_FUNCTION:nestor-charter}
nestor.planner.retirement-function=${RETIREMENT_FUNCTION:nestor-retirement}
nestor.planner.lambda-region=${DEFAULT_AWS_REGION:us-east-1}

# ── Polygon.io (market prices) ───────────────────────────────────────────────
nestor.planner.polygon-api-key=${POLYGON_API_KEY:}

logging.level.root=INFO
